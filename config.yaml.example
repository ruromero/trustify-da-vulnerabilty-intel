# Example configuration file for DA Agents
# Copy to config.yaml and modify as needed
# Set DA_AGENT_CONFIG_PATH environment variable to use a different path

retrievers:
  # Whitelist: only retrieve from these domains (empty = allow all)
  allow: []
  #  - github.com
  #  - nvd.nist.gov
  #  - example.org

  # Blacklist: never retrieve from these domains (applied after allow list)
  deny: []
  #  - bad.url.com
  #  - untrusted-source.org
